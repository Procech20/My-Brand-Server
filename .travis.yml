language: node_js

node_js:
  - 16

env:
  PORT: 5001

install:
  - 'npm install'

services:
  - mongodb

cache:
  npm: false

notifications:
  email: false

script:
  - npm ci
  - npm test
  - npm run coverage

after_success:
  - npm run coveralls
