language: node_js

node_js:
  - "stable"

install:
  - 'npm install'

services:
  - mongodb

cache:
  npm: false

notifications:
  email: false

script:
  - npm ci
  - npm test
  - npm run coverage

after_success:
  - npm run coveralls
